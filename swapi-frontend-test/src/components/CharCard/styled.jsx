import styled from "styled-components";

export const StyledContainer = styled.div`
  font-size: 24px;
  cursor: pointer;

  .cc-card__name {
    color: var(--main-yellow);
  }

  .cc-card__img {
    max-width: 100%;
  }
`;
